version: "3.9"
services:
  worker-supervisor:
    build: .                # <— constrói a imagem com o Dockerfile acima
    container_name: worker-supervisor
    environment:
      NODE_ENV: production
      DATABASE_URL: "postgres://cronos:Alice321@postgres:5432/communication"
      WORKER_POLL_MS: "10000"
    networks:
      - npm_network
    restart: unless-stopped

networks:
  npm_network:
    external: true
    name: npm_network
